#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (success)
 */

int main(void)
{
	char A;

for (A = 'a'; A <= 'z'; A++, "\n", "q, e")
if (A != 'e' && A != 'q')
putchar (A);
putchar ('\n');

return (0);
}
